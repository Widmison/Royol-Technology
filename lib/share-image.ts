/** Used for Open Graph + Twitter when links are shared (iMessage, Slack, Facebook, X, etc.). */
export const SHARE_PREVIEW_IMAGE = "/logo.jpg";

export const sharePreviewOgImage = {
  url: SHARE_PREVIEW_IMAGE,
  alt: "MEX509 logo",
} as const;
